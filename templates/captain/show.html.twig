{% extends 'base.html.twig' %}

{% block title %}Captain{% endblock %}

{% block body %}
    <h1>Captain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ captain.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ captain.name }}</td>
            </tr>
            <tr>
                <th>Dob</th>
                <td>{{ captain.dob ? captain.dob|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('captain_index') }}">back to list</a>

    <a href="{{ path('captain_edit', {'id': captain.id}) }}">edit</a>

    {{ include('captain/_delete_form.html.twig') }}
{% endblock %}